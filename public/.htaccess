<IfModule mod_rewrite.c>
  RewriteEngine On

  # SPA fallback: funciona em raiz do domínio e em subpasta
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . index.html [L]
</IfModule>
